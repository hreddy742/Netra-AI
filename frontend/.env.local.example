# Backend URL - points to the gateway service
# In Docker: http://gateway:8000
# In dev: http://localhost:8000
BACKEND_URL=http://localhost:8000

# Next.js public env vars (accessible in browser)
# These are proxied through Next.js API routes, so no direct browser access to backend needed
NEXT_PUBLIC_APP_NAME=Netra AI
